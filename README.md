# dep_safe
depsafe - A Python package dependency validator that tests compatibility in isolated Docker environments before deployment. Catch version conflicts early by inspecting PyPI dependencies and validating against specific Python versions or custom base images.
